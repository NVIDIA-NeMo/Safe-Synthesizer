<a class="metric-card" href="{{ anchor_link }}">
    <div>
        <h3>{{ name }}</h3>
        <p>{{ description }}</p>
    </div>
    <div class="metric-score">
        <strong>{{ score if score is not none else "--" }}</strong>
        <span class="score-label">{{ grade }}</span>
    </div>
</a>
